import { LeaguePipePipe } from './league-pipe.pipe';

describe('LeaguePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LeaguePipePipe();
    expect(pipe).toBeTruthy();
  });
});
